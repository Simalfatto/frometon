<%= render "shared/navbar-top-default" %>
<main class="content-middle">

<h2 class="devise-title">Crée ton compte <strong> Frometon </strong> !</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), data: { turbo: :false }) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs-signup">
    <%= f.input :email,
                required: true,
                autofocus: true,
                label: "Mon adresse email :",
                input_html: { autocomplete: "Mon email" }%>
    <%= f.input :pseudo,
                required: true,
                autofocus: true,
                label: "Mon pseudo :",
                input_html: { autocomplete: "Mon pseudo" }%>
    <%= f.input :password,
                required: true,
                label: "Mon mot de passe :",
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "Mot de passe" } %>
    <%= f.input :password_confirmation,
                required: true,
                label: "Confirmation de mon mot de passe :",
                input_html: { autocomplete: "Confirmation du mot de passe" } %>
  </div>
  <div class="form-actions-signup">
    <%= f.button :submit, "Je crée mon compte" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>

</main>
<%= render "shared/navbar-bottom-default" %>
